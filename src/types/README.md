# Types

Tipos TypeScript compartidos a nivel global. Define aquí interfaces y types que se reutilicen en múltiples capas (componentes, actions, hooks, etc.).

**No** definas tipos que sean específicos de un solo componente aquí — esos van en el mismo archivo del componente.
