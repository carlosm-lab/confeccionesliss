"use client";

/**
 * MaterialSymbolsLoader — DEPRECATED / DESACTIVADO.
 * Todos los iconos del sitio web han sido migrados 100% a SVG inline (lucide-react)
 * mediante el componente centralizado <Icon />.
 *
 * Se mantiene este componente como NOP (no-operation) para evitar desestructurar
 * layouts existentes si se importa en alguna parte.
 */
export function MaterialSymbolsLoader() {
  return null;
}
